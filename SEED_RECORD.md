# DNS シードレコード

Chromaノードは起動時に DNS シードを引き、最初の接続先を得る。
このファイルは、そのレコードの形式と現在の内容を定義する。

対応する実装は [`crates/chroma-p2p/src/discovery.rs`](crates/chroma-p2p/src/discovery.rs)。

## レコード

| 項目   | 値                    |
| ---- | -------------------- |
| 名前   | `seed.chroma.org.uk` |
| 種別   | `TXT`                |
| TTL  | 3600（1時間）            |

TXT を使う理由は、鍵をアドレスと一緒に配れるため。
Noise XK は接続先の静的鍵を **事前に** 知っていることを前提にするので、
`A` レコードのようにアドレスだけを配っても、ノードは接続すらできない。

## 形式

TXT レコードの各文字列は、次の形式ちょうど1件を表す。

```text
chroma-seed=<node-id>.<noise-key>@<host>:<port>
```

* `chroma-seed=` — 固定のプレフィックス。同じゾーンには SPF や各種検証用の
  TXT も同居するので、このプレフィックスを持たない文字列は無視される。
* `<node-id>` — ノードの **ed25519 公開鍵**（SPEC §10）。64文字の16進数。
  これがノードの同一性で、正しくなければならないのはこちら。
* `<noise-key>` — ノードの **X25519 静的公開鍵**。64文字の16進数。
  Noise XK が実際に鍵交換する相手の鍵。
* `<host>` — IPアドレス（IPv4 / IPv6）。名前は書けない。
  シードがさらに別の名前解決へ誘導できる必要はないため。
* `<port>` — TCP ポート。

鍵が2つあるのは、SPEC がノードIDを ed25519、Noise を `25519`（＝X25519）と
定めていて、これらは同じ鍵にできないため。ノードは X25519 静的鍵を自分の
ed25519 鍵で署名し、その署名をハンドシェイクの中で相手に渡す。
したがって `<noise-key>` の側が差し替えられてもハンドシェイクは成立しない
（差し替えた側はその鍵を当該 ed25519 鍵で署名できない）。信頼の起点は
あくまで `<node-id>` で、`<noise-key>` は接続に必要なヒントにすぎない。

前後の空白は許容される。プレフィックスを持たない文字列、鍵が片方しかない
文字列は、いずれも採用されずに捨てられる。

1つの TXT レコードは複数の文字列を持てるので、1レコードに複数ノードを
書ける。1回の解決で採用するのは最大 32 件（`MAX_PEERS_PER_SEED`）。
DNS の1文字列は 255 バイトまでで、1エントリは約 165 バイトなので、
1文字列 = 1エントリとして書く。

## 現在の内容

**未公開。** devnet のブートストラップノードのアドレスが確定していないため、
現時点でこのレコードは存在しない。ノードは3回失敗した時点で引くのをやめ
（`MAX_SEED_FAILURES`）、`--connect` で指定された相手だけを使う。

公開する内容が決まったら、ゾーンに次の形で登録し、この節を実際の値で
置き換える。

```zone
; <node-id>.<noise-key> は各ノードの起動ログ "Node identity: ..." から
; そのままコピーする
seed	3600	IN	TXT	"chroma-seed=<node-id>.<noise-key>@203.0.113.10:8333"
seed	3600	IN	TXT	"chroma-seed=<node-id>.<noise-key>@198.51.100.20:8333"
```

## ノードIDの取得

ノードは起動時に、`--connect` にそのまま渡せる形で自分の identity を表示する。

```console
$ chroma node --listen 0.0.0.0:8333 --data-dir /var/lib/chroma
Node identity: 9e7775e7...d035.4a1c9b02...77e1@0.0.0.0:8333
```

鍵はデータディレクトリの `node_key`（16進数32バイト、パーミッション 600）に
保存され、再起動しても変わらない。このファイルは ed25519 の種で、X25519
静的鍵もここから導出されるので、バックアップはこれ1つでよい。逆にこれを
失う、あるいは意図的に作り直すとノードIDが変わり、このレコードと
`--connect` の指定はすべて無効になる。

## 確認

```console
$ dig +short TXT seed.chroma.org.uk
"chroma-seed=9e7775e7...d035.4a1c9b02...77e1@203.0.113.10:8333"
```

実装側からの確認は、通常のテスト実行では走らない live テストで行う。

```console
$ cargo test -p chroma-p2p -- --ignored live_seed_record
```

## セキュリティ上の注意

DNS の応答は認証されていない。DNSSEC も要求していないので、経路上の
攻撃者はこのレコードの応答を差し替えられる。

差し替えられても被害が限定されるのは、レコードがアドレスではなく
**ノードID**（ed25519）を配っているからで、ノードはハンドシェイクの中で
「この X25519 静的鍵は自分のものである」という ed25519 署名を検証する。
`<noise-key>` だけを攻撃者の鍵に差し替えても、その鍵を正規の ed25519 鍵で
署名できないので接続は成立しない。攻撃者にできるのは、存在しない相手や
応答しない相手を掴ませて接続を無駄にさせることまでで、偽のピアとして
振る舞うことはできない。

採用件数に上限を設けているのも同じ理由で、応答を差し替えられたときに
ピアテーブルの大きさを相手に決めさせないため。
