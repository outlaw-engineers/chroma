# DNS シードレコード

Chromaノードは起動時に DNS シードを引き、最初の接続先を得る。
このファイルは、そのレコードの形式と現在の内容を定義する。

対応する実装は [`crates/chroma-p2p/src/discovery.rs`](crates/chroma-p2p/src/discovery.rs)。

## レコード

| 項目   | 値                    |
| ---- | -------------------- |
| 名前   | `seed.chroma.org.uk` |
| 種別   | `TXT`                |
| TTL  | 3600（1時間）            |

TXT を使う理由は、ノードIDをアドレスと一緒に配れるため。
Noise XK は接続先の公開鍵を **事前に** 知っていることを前提にするので、
`A` レコードのようにアドレスだけを配っても、ノードはその相手を認証できない。

## 形式

TXT レコードの各文字列は、次の形式ちょうど1件を表す。

```text
chroma-seed=<node-id>@<host>:<port>
```

* `chroma-seed=` — 固定のプレフィックス。同じゾーンには SPF や各種検証用の
  TXT も同居するので、このプレフィックスを持たない文字列は無視される。
* `<node-id>` — ノードの Noise 静的公開鍵。64文字の16進数（32バイト）。
* `<host>` — IPアドレス（IPv4 / IPv6）。名前は書けない。
  シードがさらに別の名前解決へ誘導できる必要はないため。
* `<port>` — TCP ポート。

前後の空白は許容される。プレフィックスを持たない文字列、ノードIDのない
`host:port` だけの文字列は、いずれも採用されずに捨てられる。

1つの TXT レコードは複数の文字列を持てるので、1レコードに複数ノードを
書ける。1回の解決で採用するのは最大 32 件（`MAX_PEERS_PER_SEED`）。
DNS の1文字列は 255 バイトまでで、1エントリは約 100 バイトなので、
1文字列 = 1エントリとして書く。

## 現在の内容

**未公開。** devnet のブートストラップノードのアドレスが確定していないため、
現時点でこのレコードは存在しない。ノードは3回失敗した時点で引くのをやめ
（`MAX_SEED_FAILURES`）、`--connect` で指定された相手だけを使う。

公開する内容が決まったら、ゾーンに次の形で登録し、この節を実際の値で
置き換える。

```zone
; <node-id> は各ノードの起動ログ "Node identity: ..." から取る
seed	3600	IN	TXT	"chroma-seed=<node-id>@203.0.113.10:8333"
seed	3600	IN	TXT	"chroma-seed=<node-id>@198.51.100.20:8333"
```

## ノードIDの取得

ノードは起動時に自分の identity を表示する。

```console
$ chroma node --listen 0.0.0.0:8333 --data-dir /var/lib/chroma
Node identity: 9e7775e714ffe142b962d5034294d1858652f8b529a46eddaf7290f215e3d035@0.0.0.0:8333
```

鍵はデータディレクトリの `node_key`（16進数32バイト、パーミッション 600）に
保存され、再起動しても変わらない。この鍵を失う、あるいは意図的に作り直すと
ノードIDが変わり、このレコードと `--connect` の指定はすべて無効になる。
シードノードでは `node_key` をバックアップしておくこと。

## 確認

```console
$ dig +short TXT seed.chroma.org.uk
"chroma-seed=9e7775e7...d035@203.0.113.10:8333"
```

実装側からの確認は、通常のテスト実行では走らない live テストで行う。

```console
$ cargo test -p chroma-p2p -- --ignored live_seed_record
```

## セキュリティ上の注意

DNS の応答は認証されていない。DNSSEC も要求していないので、経路上の
攻撃者はこのレコードの応答を差し替えられる。

差し替えられても被害が限定されるのは、レコードがアドレスではなく
**ノードID** を配っているからで、ノードは Noise XK ハンドシェイクで
その鍵を持つ相手以外との接続を成立させない。攻撃者にできるのは、
存在しない相手や応答しない相手を掴ませて接続を無駄にさせることまでで、
偽のピアとして振る舞うことはできない。

採用件数に上限を設けているのも同じ理由で、応答を差し替えられたときに
ピアテーブルの大きさを相手に決めさせないため。
